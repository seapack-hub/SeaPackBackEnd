<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.seaPack.mapper.ai.SkillCategoryMapper">

    <resultMap id="BaseMap" type="org.seaPack.model.ai.SkillCategory">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="icon" column="icon"/>
        <result property="description" column="description"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectList" resultMap="BaseMap">
        SELECT id, name, code, icon, description, sort_order, status, created_by, created_at, updated_at
        FROM ai_skill_category
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort_order ASC, created_at DESC
    </select>

    <select id="selectById" resultMap="BaseMap">
        SELECT * FROM ai_skill_category WHERE id = #{id}
    </select>

    <select id="selectByCode" resultMap="BaseMap">
        SELECT * FROM ai_skill_category WHERE code = #{code}
    </select>

    <select id="countByCode" resultType="int">
        SELECT COUNT(*) FROM ai_skill_category WHERE code = #{code}
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <insert id="insert" parameterType="org.seaPack.model.ai.SkillCategory"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_skill_category (name, code, icon, description, sort_order, status, created_by, created_at)
        VALUES (#{name}, #{code}, #{icon}, #{description}, #{sortOrder}, #{status}, #{createdBy}, NOW())
    </insert>

    <update id="update" parameterType="org.seaPack.model.ai.SkillCategory">
        UPDATE ai_skill_category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="code != null">code = #{code},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_skill_category WHERE id = #{id}
    </delete>

</mapper>
